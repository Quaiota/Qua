import React from 'react'
import styled from 'styled-components'

const Container = styled.h2`
  background: rgba(255, 255, 255, 0.02);
  font-size: 16px;
  padding: 1.2rem 1.4rem;
  border-radius: 12px;
`

const Bio = () => {
  return <Container>Add Bio</Container>
}

export default Bio
